%<@@=vZh>
\NeedsTeXFormat{LaTeX2e}
\ProvidesExplFile{}{}{}{}

\tl_new:N \@@_fontset
\tl_new:N \@@_shssuffix
\tl_new:N \kaifontname
\tl_new:N \fangfontname

\keys_define:nn {@@}{
  SHS.tl_set:N  = \@@_shssuffix,
  SHS.default:n = {SC},
  fontset.tl_set:N  = \@@_fontset,
  fontset.initial:n = {fandol},
  kai.tl_set:N   = \kaifontname,
  kai.initial:n  = {FandolKai},
  fang.tl_set:N  = \fangfontname,
  fang.initial:n = {FandolFang},
}

\ProcessKeysOptions{@@}

\PassOptionsToPackage{CJKmath}{xeCJK} % 中文支持，CJKmath公式中直接的中文
\RequirePackage[scheme=chinese, heading=true, fontset=none]{ctex}

% 字体设置
\tl_if_empty:NTF\@@_shssuffix{
  \ctexset{
    fontset=\@@_fontset,
  }
}{
  \setCJKmainfont{SourceHanSerif\@@_shssuffix}[
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Regular,
    BoldItalicFont = *-Bold
  ]
  \setCJKsansfont{SourceHanSans\@@_shssuffix}[
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Regular,
    BoldItalicFont = *-Bold
  ]
  \setCJKmonofont{SourceHanSans\@@_shssuffix}[
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Regular,
    BoldItalicFont = *-Bold
  ]
}

% 其他字体
\newCJKfontfamily\kai{\kaifontname}[
  BoldFont = *,
  ItalicFont = *,
  BoldItalicFont = *
]
\newCJKfontfamily\fang{\fangfontname}[
  BoldFont = *,
  ItalicFont = *,
  BoldItalicFont = *
]
% \RequirePackage{datetime} %日期
% \renewcommand{\today}{\number\year 年 \number\month 月 \number\day 日}
% \renewcommand\contentsname{\centering\textbf{目录}} %Contents
% \renewcommand\listfigurename{\centering\textbf{插图目录}}
% \renewcommand\listtablename{\centering\textbf{表格目录}}
% \renewcommand\refname{\centering\textbf{参考文献}}
% \renewcommand\indexname{索引}
% \renewcommand\figurename{图}
% \renewcommand\tablename{表}
% \renewcommand\abstractname{摘要}
% \renewcommand\partname{部分}
% \renewcommand\appendixname{附录}

%<@@=>